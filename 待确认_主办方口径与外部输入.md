# 待确认 · 主办方口径与外部输入

> 2026-07-06 整理，2026-07-06 FAQ 公布后更新。这些是**外部输入**（我无法通过代码/分析解决）。
> **初评提交 9 月中旬**（客观指标 CER40+RR40+效率20 排名进决赛），决赛答辩在后。

## 当前已知（2026-07-06 用户确认）
- **算力**：本机 RTX 4060（8G）+ 可借老师 3080ti（12G）+ 按量租 AutoDL。**无 A100/V100 稳定算力**。→ 不能从头训大模型，但 LoRA 微调（12G 可行）+ 推理级替换可行。
- **数据**：只有测试集 A（1838 条，**不能训**）。外部模型/数据/换基座**允许**。
- **提交**：9 月中旬 = **初评**（客观指标排名），答辩没那么快。

---

## ✅ 已由 FAQ 确认（2026-07-06 公布）

### Q1 ✅ pos 被系统拒识时，CER 怎么算？
**答**：rejected pos → CER=1.0（标准字符级 Levenshtein 编辑距离，hyp="" 时 D=N, S=I=0, CER=N/N=1.0）。**无额外惩罚（×1.5/×2 等）**。与"整段全错"扣分力度完全一致。
- 负样本不计 CER，只计 RR
- 正样本只计 CER，不计 RR
- 现有脚本 `eval_datasetA.py` / `eval_full_test.py` 已正确实现
- **对 thr 选点的影响**：误拒一条 pos = CER 贡献 1.0，thr 不能拉太高

### Q2 ✅ CER/RR → 分数 是排名制还是归一化公式？
**答**：**排名制**。CER40% + RR40% + 效率20% 加权后排名，不公布具体归一化公式。CER 和 RR 权重最高，效率在客观指标前提下综合考虑。

### Q3 🟡 neg 拒识率是句准还是 char-weighted？
**FAQ 未明确**。但自测：7 条漏拒含长新闻 23 字，char-weighted RR 与句准 98.52% 差 ≤0.1pp（漏字仅占全集等效字数 1.1%），风险极低。可不问。

### Q4 🟡 效率分（20% = 时间 10% + 内存 10%）
**FAQ 部分回答**：
- 统一 GPU 环境：**L20-46G**
- "其他算力和内存暂不做额外限制"
- **未明确**：batch_size 是否限定=1？还是允许 batch 推理？
- **不影响开发**：先按 batch=1 保证最低 RTF，L20 真测时再看能否用大 batch

---

## ✅ 已由 FAQ 确认 · 提交脚本核查

**核查方式：完整复现结果并比对（非仅看代码防作弊）**

流程：审代码合规性 → 完整复现推理 → 复测全套指标(CER/RR/效率)。**不直接用上传的 json**，而是运行 submit_infer.py 全新生成结果。

### 可复现性硬要求
| 要求 | 对我们的影响 |
|---|---|
| 零外部依赖（无私有文件/绝对路径） | 模型走 HF hub 自动下载，移除 `E:\` 路径引用 |
| 随机种子固定 | `torch.manual_seed` + `numpy` + `random` + `torch.cuda.manual_seed_all` |
| 禁用本地缓存加速 | 复测环境会清空缓存，读缓存跳过推理 = 报错/作弊 |
| 日志必须打印 | 每条音频：max_sim / batch_size / 推理耗时 / 峰值显存 / 拒识分支标识 |

### submit_infer.py 改造清单
- [ ] 固定随机种子（torch + numpy + random）
- [ ] 结构化日志（每条音频置信度/耗时/显存/分支）
- [ ] 移除本地路径引用（模型权重走 HF hub）
- [ ] 移除缓存加速逻辑
- [ ] 峰值显存日志（`torch.cuda.max_memory_allocated()`）
- [ ] DiCoW fp16 非确定性验证（seed 固定后跑两遍比对）

---

## 其他 FAQ 要点速查
| 条目 | 内容 |
|---|---|
| A 集排行榜 | 仅供参考，不决定初赛入围 |
| 入围依据 | B 集成绩 + 脚本核查 + 客观指标综合 |
| 初赛排位 | 不影响终赛排名（入围即可） |
| B 集 | 规模/时间未公布 |
| JSON 提交格式 | 不需自增字段，按示例即可 |
| 效率公式 | 不公布具体计算公式 |

---

## ⚡ L20 真测（你租 L20 后跑，回传两个数）
```bash
# 步骤1: 现有 run_baodi.sh 跑全量（batch=1 基线），另开终端记峰值显存
bash code/run_baodi.sh pos 0.4
bash code/run_baodi.sh neg 0.45
nvidia-smi --query-gpu=memory.used --format=csv -l 1 > peak_mem.csv
# 步骤2: 读 RTF
code/.venv/Scripts/python.exe -c "import json; t=json.load(open('code/out_pos_baodi/timing.json')); print('RTF=',t['overall_rtf'])"
```
**回传**：L20 的 `overall_rtf` + `峰值显存(MB)`。据此判断要不要做 DiCoW batch 改造（RTF<0.1 不改，>0.15 再 TDD 改）。
