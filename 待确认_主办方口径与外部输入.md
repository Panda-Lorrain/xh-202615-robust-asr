# 待确认 · 主办方口径与外部输入

> 2026-07-06 整理。这些是**外部输入**（我无法通过代码/分析解决），决定 thr 定值 / RR 评估 / 效率分 / 技术路线选择。
> **初评提交 9 月中旬**（客观指标 CER40+RR40+效率20 排名进决赛），决赛答辩在后。
> 拿到答案后回填，据此锁定 thr + 排攻 CER 路线。

## 当前已知（2026-07-06 用户确认）
- **算力**：本机 RTX 4060（8G）+ 可借老师 3080ti（12G）+ 按量租 AutoDL。**无 A100/V100 稳定算力**。→ 不能从头训大模型，但 LoRA 微调（12G 可行）+ 推理级替换可行。
- **数据**：只有测试集 A（1838 条，**不能训**）。外部模型/数据/换基座**允许**。
- **提交**：9 月中旬 = **初评**（客观指标排名），答辩没那么快。

---

## 🔴 Q1【最关键】pos 被系统拒识时，CER 怎么算？
**为什么关键**：直接决定 sim_thr 定值。我们实测敏感性极大：
- 若「拒识 = CER 1.0（按全错计）」：最优 thr∈[0.50, 0.55]，pos overall CER 触底 ~0.96（但该下沉在 ±0.04 运行噪声内，且 thr=0.5 时误拒率 92%、correct_rate 仅 5.9%——靠"拒=1.0"杠杆撬出，不健康）。
- 若「pos 必须转写、拒识重罚（×2 等）」：最优策略翻转为 **thr→0（尽量不拒/全转写）**。
- 同一系统、同一数据，口径不同最优配置天差地别。

**请明确**：
- (a) 拒识 pos 的 CER = 1.0？还是有额外惩罚（×1.5/×2）？
- (b) pos 是否强制要求转写（拒识算系统失败）？

## 🟡 Q2 CER → 分数 是排名制还是归一化公式？
若公式，基准 CER / 上限 CER 是多少？（关系到降 0.1 CER 值多少分，决定攻 CER 的投入度）

## 🟡 Q3 neg 拒识率是句准还是 char-weighted？
我们自测：当前 7 条漏拒含长新闻 23 字，但 char-weighted RR 与句准 98.52% 差 **≤0.1pp**（漏字仅占全集等效字数 1.1%），风险极低。但仍请确认口径。

## 🟡 Q4 效率分（20% = 时间 10% + 内存 10%）
除"每条平均推理时间 + 内存占用"外，有没有指定的 RTF 阈值或对比基线？提交时是跑全量测试集 A 计时，还是抽样？是否限定 batch_size=1？

---

## ⚡ L20 真测（你租 L20 后跑，回传两个数）
```bash
# 步骤1: 现有 run_baodi.sh 跑全量（batch=1 基线），另开终端记峰值显存
bash code/run_baodi.sh pos 0.4
bash code/run_baodi.sh neg 0.45
nvidia-smi --query-gpu=memory.used --format=csv -l 1 > peak_mem.csv
# 步骤2: 读 RTF
code/.venv/Scripts/python.exe -c "import json; t=json.load(open('code/out_pos_baodi/timing.json')); print('RTF=',t['overall_rtf'])"
```
**回传**：L20 的 `overall_rtf` + `峰值显存(MB)`。据此判断要不要做 DiCoW batch 改造（RTF<0.1 不改，>0.15 再 TDD 改）。
