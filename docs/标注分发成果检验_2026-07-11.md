# 多人标注分发成果检验（2026-07-11）

> **给下一个 agent（检验员）**：本 session（2026-07-10/11）产出一套「多人标注分发 + 比对仲裁」工具链 + CER 分布检视，已 commit + push。你的任务是**检验这些成果是否正确可用**：跑本文档第 4 节的检验脚本，对照第 5 节预期，按第 8 节模板报告 PASS/FAIL。**只检验 + 报告，不要改实现**（除非发现 bug 且被明确授权修）。

---

## 1. 背景（为什么有这些成果）

美的目标说话人 ASR 参赛（XH-202615），vanilla Whisper 路线 pos 集 CER 0.595 是天花板。要做「数据驱动错误分析」——1084 条未满分音频人工标难点 → 聚类难点分布 → 定模型路线。

需求演进（本 session）：
1. 用户原打算自己标 1084 条
2. 改为**分发给 2 个队员各标全量 1084 + 交叉比对仲裁**（防误判）
3. recognition 和 enrollment **分块标注**（enrollment 声纹唤醒词本身也有难点，如 target 处于嘈杂环境 / 有其他人说话，不能只标 recognition）
4. 「备注」→**自然语言标注**（9 类难点形容不了时自由描述，回收后交 AI 二次分类）
5. 标注界面视觉重构（卡片分层、不再拥挤）

## 2. 成果清单（6 commit，已 push origin/master，至 b377054）

| commit | 内容 |
|---|---|
| 53a6521 | 多人标注分发+比对仲裁工具链 + CER分布检视 |
| 90afb5f | AGENT_HANDOFF 加 2026-07-10 段 |
| 7b53ac0 | enrollment 区显示唤醒词参考文本(kws_txt) |
| 38d8198 | v2 recognition/enrollment 分块标注 + 自然语言标注 |
| 37bd530 | 标注界面视觉重构(卡片分层) |
| b377054 | 打包内加组员使用说明 README |

## 3. 产物文件

| 文件 | 作用 | 入库 |
|---|---|---|
| `code/build_annotator_pack.py` | 标注包生成器 v2（rec/enw 分块 + 自然语言） | ✓ |
| `code/compare_annotations.py` | 2人比对 + 争议仲裁 v2（两块比对） | ✓ |
| `code/build_cer_viewer.py` + `code/cer_distribution_viewer.html` | CER 分布检视（子 agent 生成） | ✓ |
| `code/annot_pack/`（标注.html + README.txt + pos/2168 音频） | 标注包产物 | ✗ gitignore |
| `code/annot_pack.zip`（116.6M） | 发组员的包 | ✗ gitignore |
| `memory/multi-annotator-dispatch.md` | 流程 memory（在 ~/.claude） | ✗ |

## 4. 检验脚本（一键跑，Git Bash 复制执行）

```bash
cd /e/midea_target_asr
ck(){ printf "%-32s " "$1"; }

echo "=== 检验1: 重生成标注HTML v2 ==="
code/.venv/Scripts/python.exe code/build_annotator_pack.py
F="code/annot_pack/标注.html"
ck "相对路径pos/cmd_(预期1084)"; grep -o 'pos/cmd_' "$F" | wc -l
ck "分块section(预期2)"; grep -o 'class="section' "$F" | wc -l
ck "自然语言标注label(预期2)"; grep -o '自然语言标注' "$F" | wc -l
ck "唤醒词kws_txt(预期1084)"; grep -o '"kws_txt":' "$F" | wc -l
ck "v2 store key(预期1)"; grep -o 'err_annot_v2' "$F" | wc -l
ck "卡片box-shadow(预期9)"; grep -o 'box-shadow' "$F" | wc -l

echo "=== 检验2: mock compare(预期 一致2/分歧3/未标1) ==="
code/.venv/Scripts/python.exe - <<'PY'
import csv, os
src=list(csv.DictReader(open(r"E:\midea_target_asr\code\error_analysis_pos_unfull.csv",encoding="utf-8-sig")))
uids=[r["uid"] for r in src[:6]]; idx=[x["uid"] for x in src]
td=r"E:\midea_target_asr\code\annot_pack\_verify"; os.makedirs(td,exist_ok=True)
A=[(["音量小"],["背景嘈杂"],"",""),(["babble强"],["背景嘈杂"],"",""),(["循环幻觉"],["唤醒词不清"],"",""),(["音量小"],["音量小"],"",""),(["英文干扰"],["有其他说话人"],"",""),([],[],"","")]
B=[(["音量小"],["背景嘈杂"],"",""),(["babble强"],["背景嘈杂"],"",""),(["babble强"],["唤醒词不清"],"",""),(["音量小"],["多人同说"],"",""),(["循环幻觉"],["唤醒词不清"],"",""),([],[],"","")]
for name,data in [("vA",A),("vB",B)]:
    with open(os.path.join(td,f"annot_{name}.csv"),"w",encoding="utf-8-sig",newline="") as f:
        w=csv.writer(f); w.writerow(["annotator","uid","档","CER","max_sim","rec_难点","rec_自然语言","enw_难点","enw_自然语言"])
        for uid,(rt,et,rn,en) in zip(uids,data):
            r=src[idx.index(uid)]; w.writerow([name,uid,r["档"],r["vanilla_cer"],r["max_sim"],";".join(rt),rn,";".join(et),en])
PY
code/.venv/Scripts/python.exe code/compare_annotations.py code/annot_pack/_verify/annot_vA.csv code/annot_pack/_verify/annot_vB.csv
rm -rf code/annot_pack/_verify code/annot_pack/compare

echo "=== 检验3: annot_pack.zip 完整性 ==="
ck "zip大小(预期~116M)"; ls -la code/annot_pack.zip | awk '{print $5}'
code/.venv/Scripts/python.exe -c "import zipfile; z=zipfile.ZipFile(r'code/annot_pack.zip'); print('文件数:',len(z.namelist()),'(预期2171) | 含README:',any('README'in n for n in z.namelist()),'| 含标注.html:',any('标注'in n for n in z.namelist()))"

echo "=== 检验4: CER viewer ==="
ck "viewer存在"; ls code/cer_distribution_viewer.html >/dev/null && echo OK
ck "viewer条数(预期1364)"; grep -o '"uid":' code/cer_distribution_viewer.html | wc -l

echo "=== 检验5: git状态 ==="
ck "未push commit(预期空)"; git log --oneline origin/master..HEAD | wc -l
ck "annot_pack/zip被忽略"; git check-ignore code/annot_pack code/annot_pack.zip
```

## 5. 预期结果汇总

| 项 | 预期 |
|---|---|
| 标注HTML生成 | 1084 条, recognition+enrollment 分块 v2 |
| 相对路径 pos/cmd_ | 1084 |
| 分块 section | 2 |
| 自然语言标注 label | 2 |
| 唤醒词 kws_txt | 1084（全匹配） |
| v2 store key | 1 |
| 卡片 box-shadow | 9（视觉重构） |
| mock compare | 一致 2 / 分歧 3 / 未标 1（+1079 因 mock 只标 6 条/全量 1084） |
| annot_pack.zip | ~116.6M, 2171 文件, 含 README + 标注.html |
| CER viewer | 1364 条 |
| git 未 push commit | 空 |
| annot_pack/zip 忽略 | 是（.gitignore `code/*/` + `*.zip`） |

## 6. 人工抽检（建议补做）

- 双击 `code/annot_pack/标注.html`（Firefox），确认：
  - 两块（recognition 蓝 / enrollment 橙）主题色标题栏，卡片阴影分层
  - 填名字 → ←→ 翻条 → 空格播放 recognition → 1-9 选 recognition 难点 → enrollment 难点鼠标点
  - 导出 CSV：文件名 `annot_<名字>.csv`，**8 列**（annotator/uid/档/CER/max_sim/rec_难点/rec_自然语言/enw_难点/enw_自然语言），**1084 行**
- 双击 `code/cer_distribution_viewer.html`，确认分档柱状图 + 逐条 ref/vanilla 对比 + 听音

## 7. 注意事项

- ⚠️ **工作树有一批未 commit 改动是另一个并行 agent 的在途工作**（`poc_faster_whisper.py` / `poc_qwen_asr.py` / `exp_boh_delooping.py` / `docs/前沿探索报告_2026-07-10.md` + `enroll_infer.py` / `text_utils.py` 改动 + MEMORY `cer-breakthrough-candidates` 被刷新成「19 路前沿裁决」）。**不要碰、不要 commit、不要回退**——那是前沿探索那边的，与本检验无关。
- git 手动 commit 用 **Panda_Lorrain** 身份（本机默认 `midea-overnight-loop`）：`git -c user.name=Panda_Lorrain -c user.email=2687571939@qq.com commit`
- `annot_pack/` + zip 不入库（`.gitignore`）
- 发现 bug：按第 8 节报告（哪项 FAIL + 现象 + 复现步骤），**别擅自改实现**除非被授权

## 8. 检验报告模板（填给用户）

```
检验结论: ✅ PASS / ⚠️ 部分 PASS / ❌ FAIL

自动检验（对照第5节）:
- 检验1 标注HTML v2: [PASS/FAIL]  pos/cmd_=? section=? 自然语言=? kws=? v2key=? box-shadow=?
- 检验2 mock compare: [PASS/FAIL]  实测 一致X/分歧X/未标X
- 检验3 zip:          [PASS/FAIL]  大小=? 文件数=? 含README/标注.html?
- 检验4 CER viewer:   [PASS/FAIL]  条数=?
- 检验5 git:          [PASS/FAIL]  未push=? 忽略annot_pack/zip?

人工抽检: [做了/没做] 结论

发现的问题:
1. [严重度] 现象 + 复现步骤
2. ...
（无则写"无"）
```
