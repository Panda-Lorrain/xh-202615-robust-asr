# PS4 作者请求邮件草稿（要 inference 代码 / bsrnn_legacy 模型定义）

> 目的：请求 PS4 的模型定义类 `bsrnn_legacy.py` + 推理脚本/config，以便用 HF 上发布的 checkpoint（TaurenMountain/PS4）做 zero-shot 学术对照。
> 作者：Wanyi Ning（第一作者），单位 Yijiahe（医佳禾），论文 arXiv:2607.08111，PS4 仓库 github.com/TaurenMountain/PS4。
> ⚠️ README leaderboard 已给 PS4 在 REAL-T 验证集 TER=0.639，解锁主要是实测确认（A 集大概率 0.6 量级仍不如我们 0.3436），优先级降低但邮件成本低，建议发。

## 投递渠道（建议双投）

1. **GitHub Issue（首选，公开+作者响应快）**：https://github.com/TaurenMountain/PS4/issues 新开 issue，标题用英文，正文用下方草稿
2. **arXiv 作者邮箱**：https://arxiv.org/abs/2607.08111 点 "[view email]" 拿 Wanyi Ning 邮箱，发下方邮件
3. （备选）HuggingFace `TaurenMountain/PS4` 留言

---

## 邮件 / Issue 正文（英文，专业）

**Subject**: Request for PS4 inference code / model definition (bsrnn_legacy) — academic benchmark use

Dear Wanyi Ning,

I hope this message finds you well. I am **[你的身份，如：a researcher / a participant of the Midea XH-202615 challenge]** working on target-speaker ASR — recognizing a target speaker's voice commands under noise and multi-speaker overlap, given a short enrollment.

I read your PS4 paper (arXiv:2607.08111) with great interest — congratulations on ranking **2nd overall** in the REAL-T Challenge with the best SIM and F1. The proxy-supervised joint training design (ASR cross-entropy + speaker similarity + frame-level VAD + perceptual quality) is, in our view, exactly the right direction for bridging the perception-recognition gap in TSE, and we'd like to cite it as a reference architecture in our work.

We would like to use your released PS4 checkpoint (`TaurenMountain/PS4`, `checkpoint_epoch037.pt`) for a **zero-shot academic benchmark** on our internal evaluation set, to compare against our own baseline and a Zipformer/Qwen3-ASR pipeline. However, the HuggingFace release contains only the checkpoint **without the model definition class (`bsrnn_legacy.py`) or an inference script**, and the public `REAL-TSE/wesep-real-tse` repo does not include this class — so we are currently unable to load the weights for inference (the state_dict key naming does not match the public `TSE_BSRNN_SPK` class).

Would it be possible to share:
1. The model definition class (`bsrnn_legacy.py`) and/or a minimal inference script + config, or
2. Point us to where these can be obtained?

We commit to:
- **Research-only, non-commercial** use
- **Proper citation** of your paper in any resulting report/publication
- **Not redistributing** the code or checkpoint

Thank you very much for your time, and for releasing PS4 and REAL-PS4 to the community.

Best regards,
**[你的姓名]**
**[你的单位/学校]**
**[你的邮箱]**

---

## 中文要点（供你改写）

- 身份：参赛者/研究者（美的 XH-202615 目标说话人 ASR）
- 背景： PS4 论文感兴趣，REAL-T 第2，proxy-supervised 联合训正是打通感知鸿沟的方向，想引用为参照架构
- 请求：HF 只放了 checkpoint 无 `bsrnn_legacy.py`/推理脚本，公开 wesep 没这个类，state_dict key 对不上，无法 load 做推理 → 请分享模型定义类 + 最小推理脚本/config
- 用途：zero-shot 学术对照（在我们内部评测集对比 baseline + Zipformer/Qwen3 pipeline）
- 承诺：仅研究非商用 / 引用论文 / 不再分发代码权重
